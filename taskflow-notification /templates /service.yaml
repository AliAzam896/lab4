apiVersion: v1
kind: Service
metadata:
  name: {{ include "taskflow-notification.fullname" . }}
  labels:
    {{- include "taskflow-notification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "taskflow-notification.selectorLabels" . | nindent 4 }}
